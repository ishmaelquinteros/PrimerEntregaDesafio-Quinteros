import { DTitulosDirective } from './d-titulos.directive';

describe('DTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new DTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
